import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals
// import react from react import reactdom from react dom client import index css import index css import app from app import reportwebvitals from report
// webvitals const root react dom create root document getelementbyid root root render react strictmode app react strictmode, if you want to start
// measuring performance in your app, pass a function, to log results for example reportwebvitals console.log or send t an analytics endpoint learn more https bit.ly cra vitals

reportWebVitals();
